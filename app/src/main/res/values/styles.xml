<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Modern Jetpack Compose apps do not need complex XML themes.
         We define an empty baseline theme to satisfy AndroidManifest.xml -->
    <style name="Theme.AvatarStudio" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>